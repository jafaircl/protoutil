/**
 * NATS transport entry point for `@protoutil/pubsub`.
 *
 * Install `nats` before using this transport:
 *
 * ```sh
 * npm install @protoutil/pubsub nats
 * ```
 *
 * NATS-specific client types and implementation live behind this subpath so the root
 * `@protoutil/pubsub` API remains transport-neutral.
 */
export {};
