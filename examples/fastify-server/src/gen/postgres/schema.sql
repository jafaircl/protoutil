-- schema.sql
-- Generated by protoc-gen-sql. DO NOT EDIT.
-- Engine: postgres
--
-- Source of truth for schema generation and query generation.
-- Re-generate by running: buf generate

-- library.v1.Shelf
CREATE TABLE IF NOT EXISTS "library_v1_shelf" (
    "id" BIGSERIAL NOT NULL,
    "name" TEXT NOT NULL,
    "theme" TEXT NOT NULL,
    "created_at" TIMESTAMPTZ NOT NULL DEFAULT now(),
    "updated_at" TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY ("id")
);

-- library.v1.Book
CREATE TABLE IF NOT EXISTS "library_v1_book" (
    "id" BIGSERIAL NOT NULL,
    "shelf_id" BIGINT NOT NULL,
    "name" TEXT NOT NULL,
    "author" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "read" BOOLEAN NOT NULL,
    "created_at" TIMESTAMPTZ NOT NULL DEFAULT now(),
    "updated_at" TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY ("id"),
    CONSTRAINT "fk_library_v1_book_shelf_id" FOREIGN KEY ("shelf_id") REFERENCES "library_v1_shelf" ("id")
);
CREATE INDEX IF NOT EXISTS "idx_library_v1_book_shelf_id" ON "library_v1_book" ("shelf_id");

